<%= form_for @devices_user, url: device_path(@device), html: {method: :patch} do |devices_user_form| %>
  <%= devices_user_form.collection_select :user_id, @users, :id, :to_s %>
  <%= devices_user_form.submit 'Enregistrer les changements' %>
<% end %>